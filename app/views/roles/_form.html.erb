<%= semantic_form_for [@role.project, @role] do |form| %>
  <%= form.inputs "Role" do %>
    <%= form.input :title %>
    <%= form.input :landlord %>
    <%= form.input :status, :as => :radio %>
  <% end %>

  <%= form.buttons %>
<% end %>
