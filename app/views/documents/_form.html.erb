<div class="document_edit">

<%= form_for(@document) do |f| %>
  <% if @document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@document.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <%= f.label :title %>:<br />
    <%= f.text_field :title %><br />

    <%= f.label :notes %>:<br />
    <%= f.text_area :notes %>

	<p>
    <%= f.label :listing_id %>:<br />
    <%= f.text_field :listing_id %>
   </p>
  </fieldset>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>